# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'Quadjet' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Quadjet
  pod 'atlantis-proxyman', '1.18.0'#, :configurations => ['Development', 'Development - Domestic']
  pod 'DJI-SDK-iOS', :configurations => ['Release']
  
end
